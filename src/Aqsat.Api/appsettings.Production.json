{
  "_comment": "No secrets here on purpose. ConnectionStrings__Default, Jwt__Key, Encryption__NationalIdKey and ApiIr__ApiKey are injected as environment variables (see docker-compose.prod.yml and README.md's env var table) so they never sit in source control or a container image layer.",
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Aqsat": "Information"
    }
  },
  "Deployment": {
    "ApplyMigrationsOnStartup": true
  }
}
