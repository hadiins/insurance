# Copy to .env and fill in real values. Never commit the real .env — see README.md.

SA_PASSWORD=
JWT_KEY=
NATIONAL_ID_KEY=

API_IR_KEY=
API_IR_ALLOW_PAID=false

BACKUP_RETENTION_DAYS=14

# docs/TASKS.md Task 21 (Aqsat.Updater) — deferred by default (see docs/UPDATE-SYSTEM.md); only
# needed once the update panel (Task 22) is deployed alongside it.
UPDATER_SHARED_TOKEN=
UPDATER_SIGNING_PUBLIC_KEY_PEM=
